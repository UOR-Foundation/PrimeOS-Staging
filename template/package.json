{
  "name": "{{module-name}}",
  "version": "0.1.0",
  "description": "{{module-description}}",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [
    "primeos",
    "{{module-keyword}}"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "{{parent-module}}": "^0.1.0"
  }
}
